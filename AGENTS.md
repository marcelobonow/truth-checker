# Ambiente de desenvolvimento

- Execute comandos `npm` fora do sandbox, solicitando aprovação quando
  necessário. O Node.js/npm está disponível no ambiente host do VS Code, mas
  não no `PATH` do sandbox.
